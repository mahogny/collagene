package collagene.gui.cloneAssembler.assembly;

import java.util.LinkedList;

import collagene.seq.AnnotatedSequence;

public class VectorAssembly
	{
	AnnotatedSequence targetseq;
	//What to cut with
	
	
	public LinkedList<VectorAssemblyComponent> components=new LinkedList<VectorAssemblyComponent>();
	
	
	/**
	 * 
	 * if I could choose what to insert, and then have the dialog show what sites were possible, it would be great!
	 * single-site insertion and two-site insertion. should support both. latter harder!
	 * 
	 * 
	 */
	
	}
